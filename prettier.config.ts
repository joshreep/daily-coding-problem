import type { Config } from 'prettier'

const config: Config = {
  trailingComma: 'all',
  tabWidth: 2,
  semi: false,
  singleQuote: true,
  arrowParens: 'always',
  quoteProps: 'consistent',
  printWidth: 80,
}

export default config
